/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['flowbite.com', 'webknot.in','avatars.githubusercontent.com','cdn4.iconfinder.com','images.unsplash.com','example.com', "res.cloudinary.com",],
    }
};

export default nextConfig;